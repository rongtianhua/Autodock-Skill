# 2026-04-03 Daily Notes

## 记忆维护
- 执行了记忆维护（上次 2026-04-01，间隔 2 天）
- 将 thinking 配置要点、改进建议分层、Ollama 云模型配置提炼到 MEMORY.md
# 2026-04-03 Daily Notes

## 记忆维护
- 执行了记忆维护（上次 2026-04-01，间隔 2 天）
- 将 thinking 配置要点、改进建议分层、Ollama 云模型配置提炼到 MEMORY.md

## Model 配置修正
- 发现默认模型名从 `qwen3.6-plus-preview:free` 变为 `qwen3.6-plus:free`（去掉 -preview）
- 修正了 `agents.defaults.model.primary`（`openrouter/qwen/qwen3.6-plus:free`）
- 同时修正 `agents.defaults.models` 字典（remove preview, add new）
- 用户自己用命令切会报 "model not allowed" 原因：models 字典里没加新模型
- Gateway 已重启，新会话正常

## 时区配置（双保险）
- Mac mini 系统时区设为美国东4（VPN 需要），实际用户在中国
- **方案 1（环境变量）：** LaunchAgent plist 加 `TZ=Asia/Shanghai`
- **方案 2（OpenClaw 配置）：** `envelopeTimezone: "Asia/Shanghai"` + `userTimezone: "Asia/Shanghai"`
- 两个都做了，gateway 已重启
- 用户验证时间戳正确

## Free-Ride Skill 清理
- 删除了 `Free-Ride/` 原版和 `free-ride-fixed/` 半成品
- 只保留 `free-ride-prefix-fix/`（最终修复版）

## Find-Skills Skill 安装
- 安装到全局目录：`/opt/homebrew/lib/node_modules/openclaw/skills/find-skills-skill/`
- 评分最高 (3.935)

## Playwright 技能安装 + 环境
- 安装 `playwright-browser-automation`（全局）和 `playwright-cli-openclaw`（全局）
- 安装 Playwright npm 包 + Chromium 浏览器引擎
- 已安装并可执行

## Puff Agent 权限管理
- 给 puff 添加了：`exec`、`cron`、`image`、`image_generate` 到 allow 列表
- 每次修改后都重启 gateway
- puff 工具权限配置记录到 `memory/guides/multi-agent-guide.md`

## akshare-stock 网络问题调查
- TUN 模式让 akshare 的所有请求走代理，导致 `ProxyError`
- 关闭 TUN 后改用系统代理 + 环境变量
- 生成了 `proxy-config.zshrc.example` 备用（含代理 + no_proxy 白名单）
- no_proxy 包含 `.cn`, `.eastmoney.com`, `localhost` 等国内/本地域名
- baostock 测试通过，可作 akshare 失败时的 fallback
- Fallback 逻辑已写入 akshare-stock SKILL.md，同时同步到 puff workspace
