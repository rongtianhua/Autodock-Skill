# 2026-04-05 (周日)

## FreeRide Prefix Fix 更新发布

### 修复内容
- **问题**：`get_api_key()` 只检查环境变量和 `openclaw.json` 的 `env` 字段，没有检查 agent 的 `auth-profiles.json`（OpenClaw 实际存储 API key 的地方）
- **表现**：`./main.py list`、`status`、`switch` 等命令报 `OPENROUTER_API_KEY not set`
- **修复**：`main.py` 的 `get_api_key()` 增加扫描 `~/.openclaw/agents/*/agent/auth-profiles.json` 的逻辑
- **版本**：1.0.7 → 1.0.8

### CLI 发布
- slug: `free-ride`
- `clawhub publish ./free-ride-prefix-fix --slug free-ride --name "Free Ride - Free AI for OpenClaw" --version 1.0.8 --changelog "Fix: get_api_key() now reads auth-profiles.json for OpenRouter key"`

## 配置修复汇总

### 微信通道恢复
- `plugins.entries.openclaw-weixin.enabled: true` + `channels.openclaw-weixin` 写入 `openclaw.json`
- 微信消息恢复正常

### Puff Agent 重建
- 重建 `agents.list`（main + puff）
- puff 绑定 `openclaw-weixin` → `c34be62a65ea-im-bot`
- puff 权限：18 工具全开（含 exec/cron/image），deny process + gateway
- puff 心跳开启：30min 轮询
