# 2026-04-04 (Sat)

## Tailscale 远程访问配置
- 用户安装 Tailscale，想远程访问 OpenClaw Dashboard
- Funnel 未启用（需去 Tailscale Admin 开启）
- 改用 Serve 模式：`tailscale serve --bg http://localhost:18789`
- Dashboard 地址：`https://allens-mac-mini.tail1b1f0d.ts.net`
- `dangerouslyDisableDeviceAuth: true` 用于跨设备登录

## SSH 远程终端
- 开启系统设置 → 远程登录
- 用户名是 `allenrong`（不是 `allen`，也不是 macOS 显示名 `Allen Rong`）
- 通过 Tailscale SSH：`ssh allenrong@allens-mac-mini`

## OpenClaw 版本更新导致配置丢失
- 更新 OpenClaw 后 `openclaw.json` 的 channels 配置被清空
- 微信不受影响（独立扩展，账户文件在 `extensions/openclaw-weixin/`）
- 飞书配置丢失（内置扩展，配置在 openclaw.json channels 字段）
- 手动恢复飞书配置到 openclaw.json
- **教训**：`openclaw configure` 向导会覆盖整个 channels 字段，不要随便跑

## 模型配置
- Primary: `openrouter/qwen/qwen3.6-plus:free`
- Fallback: `kimi/kimi-code` → `ollama/kimi-k2.5:cloud` → `openrouter/stepfun/step-3.5-flash:free` → `openrouter/nvidia/nemotron-3-super-120b-a12b:free` → `openrouter/minimax/minimax-m2.5:free`
- thinkingDefault: medium
- 从模型列表删除了 `openrouter/auto`
- `claude-opus-4-6` 不是本地配置，是 OpenRouter auto 路由的后端映射

## 记忆维护（2026-04-05 心跳）
- 执行了记忆维护，将 Tailscale 远程访问、SSH、版本更新配置丢失、模型配置等经验提炼到 MEMORY.md

## 配置文件路径
- 主配置：`~/.openclaw/openclaw.json`
- Agent 运行时模型缓存：`~/.openclaw/agents/[name]/agent/models.json`（自动生成，不需手动改）
- 微信账户：`~/.openclaw/openclaw-weixin/accounts/`
