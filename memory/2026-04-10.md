
## 16:55-17:00 网关故障排查

### 根因
- gateway.err.log 膨胀到 762MB（config dump），导致 I/O 压力
- 主会话 c8a143f7 早期有 edit openclaw.json 失败重试记录（fallbacks 字段）
- 多次 SIGTERM → shutdown → launchd KeepAlive 重启，端口冲突导致连锁故障

### 处理结果
- ✅ gateway.err.log 已清空
- ✅ config-guardian 不是元凶（只是建议者，无自动执行）
- ✅ hybrid 模式保持不变（hot 更激进，不改）
- ⚠️ 主会话需重置以清除残留问题状态

### 待完成
- MemOS 插件安装（memos-local-openclaw）

## 21:47 心跳维护
- MemOS 插件安装完成并配置（bge-m3 embedding + MiniMax-M2.7 summarizer）
- memory-lancedb 配置已清理（allow list + entries）
- elite-longterm-memory 技能已删除（136MB）
- memory-core 内置 Dreaming 未启用（与 MemOS 冲突，保留 MemOS）
- 定时备份执行：成功，12个备份保留
